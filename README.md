# MASK_NONMASK_DECTECTION_CNN
This project uses a Convolutional Neural Network (CNN) to classify images as mask or non-mask. The model learns facial features from images to automatically detect whether a person is wearing a face mask, supporting applications in public safety and health monitoring.
